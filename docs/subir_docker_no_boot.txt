# parar e remover o container antigo
docker stop qdrant
docker rm qdrant

# recriar com restart automático
docker run -d --name qdrant \
  --restart unless-stopped \
  -p 6333:6333 \
  -v ~/qdrant_storage:/qdrant/storage \
  qdrant/qdrant:latest

